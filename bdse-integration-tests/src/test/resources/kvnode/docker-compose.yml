version: '3'
services:
  db:
    image: postgres:10
    volumes:
      - db-data:/var/lib/postgresql/data

  kvnode:
    depends_on:
      - db
    build: .
    ports:
      - 8080

volumes:
  db-data: